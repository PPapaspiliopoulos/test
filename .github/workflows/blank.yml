name: build

on: [push]

jobs:
  A:
    runs-on: ubuntu-latest
    environment: ${{ ('prod') || ('dev') }}
    steps:
      - name: Check identity
        run: |
          echo "A prod = ${{ vars.A }}"
          echo "B dev = ${{ vars.B }}"
